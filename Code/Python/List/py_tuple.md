Similar to list, only difference is that tuple uses ==**"()" parenthesis**== to define, here look below:
```python
list = (1,2,"hello")
```
This can't be modified anyway as like we do in list.
```python
list[0]=3
```
This will throw an error.